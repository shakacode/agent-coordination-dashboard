#!/usr/bin/env bash
# Generated by shakacode/agent-workflows agent-workflow-seam-doctor --init.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
npm run typecheck -- "$@"
# The attention contract test drives the real agent-coord CLI against a
# throwaway state root. It skips with a visible reason when agent-coord or ruby
# is not on PATH, which is what hosted CI runs, so the pre-push gate is the only
# place the CLI-to-reader contract is actually exercised.
exec npm test -- src/server/attention/contract.test.ts
